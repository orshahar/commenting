<%= form_for(@comment_thread) do |f| %>
    <% if @comment_thread.errors.any? %>
        <div id="error_explanation">
            <h2><%= pluralize(@comment_thread.errors.count, "error") %> prohibited this comment_thread from being
                saved:</h2>

            <ul>
                <% @comment_thread.errors.full_messages.each do |msg| %>
                    <li><%= msg %></li>
                <% end %>
            </ul>
        </div>
    <% end %>

    <div class="field">
        <%= f.label :title %><br/>
        <%= f.text_field :title %>
    </div>
    <div class="field">
        <%= f.label :body %><br/>
        <%= f.text_area :body %>
    </div>
    <div class="hidden">
        <%= f.hidden_field :user_id %>
    </div>
    <div class="actions">
        <%= f.submit submit_text %>
    </div>
<% end %>
